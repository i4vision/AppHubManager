# App Launcher Environment Variables Template
# Copy this file to .env and fill in your actual values

# ===== Database Configuration =====
# Your self-hosted Supabase PostgreSQL database
POSTGRES_HOSTNAME=10.115.14.71
POSTGRES_DB=your_database_name
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_database_password

# Optional: Only set if your database uses a non-standard port
# Leave commented out to connect without specifying port
# POSTGRES_PORT=5432

# ===== Application Secrets =====
# Secret code required to add new apps (choose a strong, random string)
ACCESS_CODE=your_secret_access_code_here

# Optional: Session secret for Express sessions (generate a random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# SESSION_SECRET=your_session_secret_here

# ===== Node Environment =====
# Automatically set by Docker Compose, no need to set in .env
# NODE_ENV=production
